<?xml version="1.0"?>
<robot name="ur3_with_robotiq_2f_85" xmlns:xacro="http://www.ros.org/wiki/xacro">

  <!-- UR3 기본 모델 포함 -->
  <xacro:include filename="$(find ur_description)/urdf/ur_macro.xacro"/>
  <!-- Robotiq 2F-85 공식 패키지 (ros-humble-robotiq-description) -->
  <xacro:include filename="$(find robotiq_description)/urdf/robotiq_2f_85_macro.urdf.xacro"/>

  <!-- UR3 파라미터 -->
  <xacro:arg name="ur_type" default="ur3"/>
  <xacro:arg name="tf_prefix" default=""/>
  <xacro:arg name="joint_limit_params"
    default="$(find ur_description)/config/$(arg ur_type)/joint_limits.yaml"/>
  <xacro:arg name="kinematics_params"
    default="$(find ur_description)/config/$(arg ur_type)/default_kinematics.yaml"/>
  <xacro:arg name="physical_params"
    default="$(find ur_description)/config/$(arg ur_type)/physical_parameters.yaml"/>
  <xacro:arg name="visual_params"
    default="$(find ur_description)/config/$(arg ur_type)/visual_parameters.yaml"/>
  <xacro:arg name="safety_limits" default="false"/>
  <xacro:arg name="safety_pos_margin" default="0.15"/>
  <xacro:arg name="safety_k_position" default="20"/>

  <!-- 월드 링크 -->
  <link name="world"/>
  <joint name="world_joint" type="fixed">
    <parent link="world"/>
    <child link="$(arg tf_prefix)base_link"/>
    <origin xyz="0 0 0" rpy="0 0 0"/>
  </joint>

  <!-- UR3 로봇 생성 -->
  <xacro:ur_robot
    name="$(arg ur_type)"
    tf_prefix="$(arg tf_prefix)"
    parent="world"
    joint_limits_parameters_file="$(arg joint_limit_params)"
    kinematics_parameters_file="$(arg kinematics_params)"
    physical_parameters_file="$(arg physical_params)"
    visual_parameters_file="$(arg visual_params)"
    safety_limits="$(arg safety_limits)"
    safety_pos_margin="$(arg safety_pos_margin)"
    safety_k_position="$(arg safety_k_position)">
    <origin xyz="0 0 0" rpy="0 0 0"/>
  </xacro:ur_robot>

  <!-- Robotiq 2F-85 그리퍼 부착 (GAZEBO_ENV fortress_ur3_robotiq 동일 방식) -->
  <xacro:robotiq_gripper
    name="robotiq_2f_85"
    prefix="$(arg tf_prefix)"
    parent="$(arg tf_prefix)tool0"
    use_fake_hardware="true">
    <origin xyz="0 0 0" rpy="0 0 0"/>
  </xacro:robotiq_gripper>

  <!-- TCP 프레임: 두 핑거팁 사이 중점 (파지 기준점) -->
  <!-- robotiq_85_base_link 기준 Z축 방향 약 0.13m -->
  <link name="$(arg tf_prefix)robotiq_85_tcp"/>
  <joint name="$(arg tf_prefix)robotiq_85_tcp_joint" type="fixed">
    <parent link="$(arg tf_prefix)robotiq_85_base_link"/>
    <child  link="$(arg tf_prefix)robotiq_85_tcp"/>
    <origin xyz="0 0 0.13" rpy="0 0 0"/>
  </joint>

</robot>
